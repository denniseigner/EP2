public interface StringIteratorIterable extends java.lang.Iterable<StringIterator> {
    // creates an iterator for the container 'this'
    StringIteratorIterator iterator();
}
                                
